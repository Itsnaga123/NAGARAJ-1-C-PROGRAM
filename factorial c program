#include<stdio.h>
int factorial(int n)
if(n==0) {
  return 1;
}
return n*factorical(n-1);
}
int main()
int num;
printf("enter a number:");
scanf("%d",&num);
printf("factorical=%d\n",factorial(num));
return 0;
}
}
